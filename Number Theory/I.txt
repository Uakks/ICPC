#include <iostream>
#include <cmath>
#include <algorithm>
#include <string>
#include <vector>
#include <map>
#include <iomanip>

using namespace std;

int main()
{
    long long t, n;
    cin >> t >> n;
    cout << fixed << long(pow(t, n));
}
